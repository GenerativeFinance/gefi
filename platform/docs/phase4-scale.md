# Phase 4 — scale (deferred)

Only after profiling and product fit:

- RISC Zero / SP1 for general protocol proofs (not full large NN training)
- Selective homomorphic encryption for small aggregates / scoring
- TEEs as complementary attestation (not a ZK substitute)
- Rust/Wasm numerical kernels for proven hotspots
- Dedicated vector DB / event bus / Kubernetes / GPU — when load demands

Do not start Phase 4 work before Phases 1–3 are measurable in staging.
