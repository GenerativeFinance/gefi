# Circuit placeholders for EZKL / Halo2

- `client-update/` — prove clip + model-hash + update commitment binding
- `aggregate/` — prove cohort threshold + aggregate binding
- `evaluation/` — optional metric integrity

Compile ONNX → EZKL settings here when promoting stubs to real proofs.
Artifacts from `services/zk-prover` land under `*/artifacts/` (gitignored).
