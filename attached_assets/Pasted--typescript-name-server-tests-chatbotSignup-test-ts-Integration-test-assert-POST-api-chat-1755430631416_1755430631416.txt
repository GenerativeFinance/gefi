```typescript name=server/tests/chatbotSignup.test.ts
/**
 * Integration test: assert POST /api/chatbot/signup/complete creates a pending user
 *
 * Notes:
 * - This test uses supertest against an Express app instance where we register
 *   the chatbot routes directly.
 * - We mock the server storage module to observe that upsertUser is called with
 *   a user object that has status === 'pending'.
 *
 * Requirements:
 * - Jest (or compatible test runner) + ts-jest
 * - supertest
 *
 * Run (example):
 *   npx jest server/tests/chatbotSignup.test.ts
 *
 * If your CI uses a different runner, adapt accordingly.
 */

import express from "express";
import request from "supertest";

jest.mock("../storage", () => {
  // simple in-memory spy implementation of storage used by server routes
  const upsertUser = jest.fn(async (user: any) => {
    // emulate DB returning the created user with an id
    return { id: "mock_user_id", ...user };
  });

  const getUserByEmail = jest.fn(async (email: string) => {
    return null;
  });

  return {
    storage: {
      upsertUser,
      getUserByEmail,
      // add any other storage methods the route might call as no-op mocks
      createOrUpdateUserProfile: jest.fn(async () => ({})),
      // expose the spies so tests can assert on them
    },
  };
});

describe("POST /api/chatbot/signup/complete", () => {
  let app: express.Express;
  let storageMock: any;

  beforeAll(async () => {
    // require after jest.mock to ensure the mocked storage is used by the route module
    const { registerChatbotRoutes } = await import("../routes/chatbotRoutes");

    app = express();
    app.use(express.json());

    // register only chatbot routes (keeps test fast and focused)
    registerChatbotRoutes(app);

    // import the mocked storage to assert calls
    // eslint-disable-next-line @typescript-eslint/no-var-requires
    const storageModule = require("../storage");
    storageMock = storageModule.storage;
  });

  beforeEach(() => {
    jest.clearAllMocks();
  });

  it("creates a pending user and persists via storage.upsertUser", async () => {
    const payload = {
      email: "tester+ci@example.com",
      firstName: "CI",
      lastName: "Tester",
      role: "investor",
      company: "TestCo",
      experienceLevel: "Beginner",
      // include fields the route may expect
      sessionId: "test-session-123",
      recaptchaToken: "test-token",
      honeypot: ""
    };

    const res = await request(app).post("/api/chatbot/signup/complete").send(payload);

    // expect HTTP success (200-299)
    expect(res.status).toBeGreaterThanOrEqual(200);
    expect(res.status).toBeLessThan(300);

    // storage.upsertUser should have been called to persist the new user
    expect(storageMock.upsertUser).toHaveBeenCalled();
    const createdArg = storageMock.upsertUser.mock.calls[0][0];

    // assert key fields were passed and the user is created with pending status
    expect(createdArg).toMatchObject({
      email: payload.email,
      firstName: payload.firstName,
      lastName: payload.lastName,
    });

    // Important: chatbot signup should create accounts with status 'pending'
    expect(createdArg.status).toBe("pending");
  });
});
```

Notes / next steps
- I added a focused integration test that:
  - Starts an Express app.
  - Registers the chatbot routes (so we exercise the actual route handler logic).
  - Mocks the server storage module to observe persistence calls.
  - Asserts that the handler returns success and that storage.upsertUser was called with a user object whose status is "pending".
- Run it with your repo's test command (example):
  - npx jest server/tests/chatbotSignup.test.ts
  - or npm test (if your test script runs Jest).
- If your CI uses a different test runner (Vitest / Mocha), I can adapt the test to that runner.
- If the actual chatbot route depends on additional middleware (rate limiter, security middleware) which interferes with the test, we can:
  - either mock/disable those middlewares in test (by injecting stubbed implementations),
  - or register the full route chain (as done in your server startup) to better mirror production.
- Want me to:
  - Add this test to the PR with the previously prepared ChatbotSignup and authCompat changes and generate the PR body for you? I can produce the ready-to-paste branch diff and PR description.