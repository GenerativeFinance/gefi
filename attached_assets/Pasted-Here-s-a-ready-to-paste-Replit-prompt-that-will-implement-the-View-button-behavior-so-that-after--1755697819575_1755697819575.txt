Here’s a ready-to-paste Replit prompt that will implement the “View” button behavior so that, after a subscription is Active, clicking View opens the model’s full UI (canonical path or dedicated page; otherwise fallback to /ai-models/:id).

Replit task
- Update the My Subscriptions page so the “View” action resolves to the model’s correct full UI route:
  1) If the model has canonicalPath or slug, use it.
  2) Else handle known dedicated pages:
     - id === 5 → /forecasting-model
     - id === 9 → /hrp-portfolio-optimization
  3) Else fallback to /ai-models/:id
- Prefer client-side navigation with wouter’s navigate; fallback to window.location.href.
- Keep the UI and button label intact; only change behavior.
- Do not regress pause/resume/cancel flows.

Files and changes
1) client/src/pages/my-subscriptions.tsx
- Add useLocation import.
- Add a handleViewModel function that:
  - Checks known custom routes first.
  - Fetches /api/ai-models/:id; if the model has canonicalPath or slug, route there.
  - Else fallback to /ai-models/:id.
- Replace the current Link-based “View” with a Button onClick={() => handleViewModel(subscription.modelId)}.

Apply these edits:

```typescript name=client/src/pages/my-subscriptions.tsx
// 1) Add this import near the top with other imports
import { useLocation } from 'wouter';
```

```typescript name=client/src/pages/my-subscriptions.tsx
// 2) Inside the component (e.g., near other hooks), add:
const [, navigate] = useLocation();

async function handleViewModel(id: number) {
  // Known dedicated pages
  if (id === 5) {
    try { navigate('/forecasting-model'); } catch { window.location.href = '/forecasting-model'; }
    return;
  }
  if (id === 9) {
    try { navigate('/hrp-portfolio-optimization'); } catch { window.location.href = '/hrp-portfolio-optimization'; }
    return;
  }

  // Try to fetch details to discover canonicalPath/slug
  try {
    const resp = await fetch(`/api/ai-models/${id}`);
    if (resp.ok) {
      const model = await resp.json();
      const canonicalPath = (model?.canonicalPath && String(model.canonicalPath)) || '';
      const slug = (model?.slug && String(model.slug)) || '';

      const target = canonicalPath
        ? `/${canonicalPath.replace(/^\//, '')}`
        : slug
          ? `/models/${slug.replace(/^\//, '')}`
          : `/ai-models/${id}`;

      try { navigate(target); } catch { window.location.href = target; }
      return;
    }
  } catch (e) {
    // no-op; fall through to default
  }

  // Fallback: standard model detail route
  const fallback = `/ai-models/${id}`;
  try { navigate(fallback); } catch { window.location.href = fallback; }
}
```

```typescript name=client/src/pages/my-subscriptions.tsx
// 3) Find the View button in the subscription card actions (was using `asChild` Link).
// Replace that block with this button while keeping classes/variant consistent:

<Button
  variant="outline"
  size="sm"
  onClick={() => handleViewModel(subscription.modelId)}
>
  <BarChart3 className="h-4 w-4 mr-1" />
  View
</Button>
```

Acceptance criteria
- From My Subscriptions → Active, clicking “View”:
  - For model id 5 opens /forecasting-model.
  - For model id 9 opens /hrp-portfolio-optimization.
  - For other models, opens canonicalPath/slug if available; otherwise /ai-models/:id.
- No changes to visuals; only behavior is improved.
- Pause/Resume/Cancel continue to function, no new console errors.

Notes from repo context
- The current View implementation uses Link href={`/model/${subscription.modelId}`}, which does not always reach dedicated UIs.
- Marketplace and ModelDetail already contain special-case redirects; this brings My Subscriptions in line with that behavior, but by preferring canonicalPath/slug when available.
- Server routes for /api/ai-models/:id already exist and return detailed model info.

If you want to centralize route resolution for reuse, I can provide a client/src/lib/modelRoutes.ts helper next.