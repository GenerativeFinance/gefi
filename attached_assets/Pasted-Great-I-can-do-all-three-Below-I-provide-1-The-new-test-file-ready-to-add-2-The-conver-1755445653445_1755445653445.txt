Great — I can do all three. Below I provide:

- 1) The new test file (ready to add).
- 2) The converted navigation changes for the two files (header + marketplace) using wouter navigate() instead of window.location.href.
- 3) Exact git + gh commands to create the branch, add the files, commit, push and open a PR.
- 4) Notes about running the tests & CI caveats.

If you prefer, I can instead produce a single unified patch file you can apply with git apply — say so and I’ll generate the unified diff. For now I’m providing the concrete files (safe to add/replace) and the exact commands to make the PR.

1) New test file (add)
```typescript name=client/src/pages/__tests__/model-detail.test.tsx
import React from "react";
import { render, waitFor, screen } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import ModelDetail from "../model-detail";

// Mock the apiRequest helper used by ModelDetail
jest.mock("@/lib/queryClient", () => ({
  apiRequest: jest.fn(),
}));

const { apiRequest } = require("@/lib/queryClient");

function createQueryClient() {
  return new QueryClient({
    defaultOptions: {
      queries: {
        retry: false,
      },
    },
  });
}

describe("ModelDetail id param resolution & query enabling", () => {
  beforeEach(() => {
    jest.clearAllMocks();
    // default mock returns ok response with JSON body
    apiRequest.mockImplementation(async (url: string, method = "GET") => {
      // return a Response-like object for most tests
      return {
        ok: true,
        status: 200,
        json: async () => ({ id: parseInt(String(url).split("/").pop() || "0", 10), name: "Test Model" }),
      };
    });
  });

  it("resolves id from /model/:id and calls the API", async () => {
    // set location to /model/5
    window.history.pushState({}, "Test", "/model/5");

    const qc = createQueryClient();
    render(
      <QueryClientProvider client={qc}>
        <ModelDetail />
      </QueryClientProvider>
    );

    await waitFor(() => {
      expect(apiRequest).toHaveBeenCalledWith("/api/ai-models/5", "GET");
    });

    // basic smoke: model title rendered
    await waitFor(() => {
      expect(screen.getByText(/Test Model/i)).toBeInTheDocument();
    });
  });

  it("resolves id from /marketplace/:id and calls the API", async () => {
    window.history.pushState({}, "Test", "/marketplace/7");

    const qc = createQueryClient();
    render(
      <QueryClientProvider client={qc}>
        <ModelDetail />
      </QueryClientProvider>
    );

    await waitFor(() => {
      expect(apiRequest).toHaveBeenCalledWith("/api/ai-models/7", "GET");
    });

    await waitFor(() => {
      expect(screen.getByText(/Test Model/i)).toBeInTheDocument();
    });
  });

  it("falls back to pathname parsing for /models/:id and calls the API", async () => {
    window.history.pushState({}, "Test", "/models/9?something=true");

    const qc = createQueryClient();
    render(
      <QueryClientProvider client={qc}>
        <ModelDetail />
      </QueryClientProvider>
    );

    await waitFor(() => {
      expect(apiRequest).toHaveBeenCalledWith("/api/ai-models/9", "GET");
    });

    await waitFor(() => {
      expect(screen.getByText(/Test Model/i)).toBeInTheDocument();
    });
  });
});
```

2) Header change (canonical navigation via wouter navigate)
/client/src/components/layout/header-broken.tsx — patch the search select handler
- Find the handler that currently does navigate(`/marketplace?model=${modelId}`)
- Replace it with navigate(`/model/${modelId}`)

Example snippet to replace in that file (apply the change in your file where handleSearchSelect is defined):
```tsx name=client/src/components/layout/header-broken.tsx
// old:
// const handleSearchSelect = (modelId: number) => {
//   setSearchOpen(false);
//   setSearchQuery("");
//   navigate(`/marketplace?model=${modelId}`);
// };

// new:
const handleSearchSelect = (modelId: number) => {
  setSearchOpen(false);
  setSearchQuery("");
  // Navigate to canonical model detail route
  navigate(`/model/${modelId}`);
};
```

Note: header-broken already used navigate from wouter in the snippet I scanned — this just updates the target route.

3) Marketplace change (use wouter navigate instead of location.href)
- Add useLocation import from wouter if not present
- Replace window.location.href or direct href navigation to SPA route via navigate()

Example replacement to apply in client/src/pages/marketplace.tsx:

Add (near other imports):
```tsx name=client/src/pages/marketplace.tsx
import { useLocation } from "wouter";
```

Then find or add the handleModelClick function and use navigate:
```tsx name=client/src/pages/marketplace.tsx
const [, navigate] = useLocation();

const handleModelClick = (model: any) => {
  // preserve special-case redirects for specific models
  if (model.id === 5) {
    // these are special pages — keep full page navigation if required
    window.location.href = "/forecasting-model";
    return;
  } else if (model.id === 9) {
    window.location.href = "/hrp-portfolio-optimization";
    return;
  }
  // nav to canonical model detail SPA route
  navigate(`/model/${model.id}`);
};
```

Make sure model cards use this handler:
- In card onClick: onClick={() => handleModelClick(model)} (the code already uses that in the repo).

4) Exact git + gh commands to create branch, add files, commit, push and open PR

Run these commands from your repo root:

- Create the branch:
  git checkout -b feat/tests/model-id-resolution

- Add the new test file (save the test file content to client/src/pages/__tests__/model-detail.test.tsx). Apply the header/marketplace modifications described above.

- Stage & commit:
  git add client/src/pages/__tests__/model-detail.test.tsx client/src/components/layout/header-broken.tsx client/src/pages/marketplace.tsx
  git commit -m "test(model-detail): add idParam resolution tests; standardize model routing to /model/:id"

- Push branch:
  git push --set-upstream origin feat/tests/model-id-resolution

- Create PR (with gh CLI):
  gh pr create --title "test(model-detail): idParam resolution tests + standardize model navigation" --body "Adds an integration-style test that asserts ModelDetail resolves id from /model/:id, /marketplace/:id and /models/:id. Standardizes navigation to canonical /model/:id from header search and marketplace cards. Keeps existing special-case redirects for forecasting & HRP pages." --base main

If you prefer I can run the gh command for you — provide the GitHub CLI access or run it locally.

5) Run the tests locally / CI notes
- Ensure test deps are installed. If you don't already have testing setup for the client:
  npm install --save-dev @testing-library/react @testing-library/jest-dom jest babel-jest @types/jest
  or
  yarn add -D @testing-library/react @testing-library/jest-dom jest babel-jest @types/jest

- Run:
  npx jest client/src/pages/__tests__/model-detail.test.tsx
  or run your repository's test command (npm test / yarn test).

- If Jest doesn't resolve "@/..." path alias, update jest.config.js moduleNameMapper to map "@" to the client/src path, or change the test mock to require the relative path instead:
  jest.mock("../../lib/queryClient", () => ({ apiRequest: jest.fn() }));

6) Want a single git patch (diff) file instead?
- I can generate a single unified diff (.patch) you can apply with git apply. Tell me and I’ll produce the .patch containing:
  - add client/src/pages/__tests__/model-detail.test.tsx
  - modifications to client/src/components/layout/header-broken.tsx
  - modifications to client/src/pages/marketplace.tsx

7) Want me to open the PR for you?
- I can create the PR via gh CLI if you run the gh command locally, or if you prefer I can provide the exact gh command (above). I cannot run gh from here without access to your environment.

Next step
- Which do you want now?
  A) I should generate the single git patch (.patch) now for git apply (I will produce it),
  B) I should open the PR for you (I will provide the gh command or run if you give access), or
  C) Apply the code changes to the repo and create the PR (I can produce the full file replacements in code blocks so you can paste them in).

If you want the .patch (option A), I’ll generate the unified diff file now.