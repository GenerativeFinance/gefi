```diff name=feat-tests-model-id-resolution.patch
*** Begin Patch
*** Add File: client/src/pages/__tests__/model-detail.test.tsx
+import React from "react";
+import { render, waitFor, screen } from "@testing-library/react";
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import ModelDetail from "../model-detail";
+
+// Mock the apiRequest helper used by ModelDetail
+jest.mock("@/lib/queryClient", () => ({
+  apiRequest: jest.fn(),
+}));
+
+const { apiRequest } = require("@/lib/queryClient");
+
+function createQueryClient() {
+  return new QueryClient({
+    defaultOptions: {
+      queries: {
+        retry: false,
+      },
+    },
+  });
+}
+
+describe("ModelDetail id param resolution & query enabling", () => {
+  beforeEach(() => {
+    jest.clearAllMocks();
+    // default mock returns ok response with JSON body
+    apiRequest.mockImplementation(async (url: string, method = "GET") => {
+      // return a Response-like object for most tests
+      return {
+        ok: true,
+        status: 200,
+        json: async () => ({ id: parseInt(String(url).split("/").pop() || "0", 10), name: "Test Model" }),
+      };
+    });
+  });
+
+  it("resolves id from /model/:id and calls the API", async () => {
+    // set location to /model/5
+    window.history.pushState({}, "Test", "/model/5");
+
+    const qc = createQueryClient();
+    render(
+      <QueryClientProvider client={qc}>
+        <ModelDetail />
+      </QueryClientProvider>
+    );
+
+    await waitFor(() => {
+      expect(apiRequest).toHaveBeenCalledWith("/api/ai-models/5", "GET");
+    });
+
+    // basic smoke: model title rendered
+    await waitFor(() => {
+      expect(screen.getByText(/Test Model/i)).toBeInTheDocument();
+    });
+  });
+
+  it("resolves id from /marketplace/:id and calls the API", async () => {
+    window.history.pushState({}, "Test", "/marketplace/7");
+
+    const qc = createQueryClient();
+    render(
+      <QueryClientProvider client={qc}>
+        <ModelDetail />
+      </QueryClientProvider>
+    );
+
+    await waitFor(() => {
+      expect(apiRequest).toHaveBeenCalledWith("/api/ai-models/7", "GET");
+    });
+
+    await waitFor(() => {
+      expect(screen.getByText(/Test Model/i)).toBeInTheDocument();
+    });
+  });
+
+  it("falls back to pathname parsing for /models/:id and calls the API", async () => {
+    window.history.pushState({}, "Test", "/models/9?something=true");
+
+    const qc = createQueryClient();
+    render(
+      <QueryClientProvider client={qc}>
+        <ModelDetail />
+      </QueryClientProvider>
+    );
+
+    await waitFor(() => {
+      expect(apiRequest).toHaveBeenCalledWith("/api/ai-models/9", "GET");
+    });
+
+    await waitFor(() => {
+      expect(screen.getByText(/Test Model/i)).toBeInTheDocument();
+    });
+  });
+});
+
*** End Patch
*** End Patch
*** Begin Patch
*** Update File: client/src/components/layout/header-broken.tsx
@@
-  const handleSearchSelect = (modelId: number) => {
-    setSearchOpen(false);
-    setSearchQuery("");
-    navigate(`/marketplace?model=${modelId}`);
-  };
+  const handleSearchSelect = (modelId: number) => {
+    setSearchOpen(false);
+    setSearchQuery("");
+    // Navigate to canonical model detail route
+    navigate(`/model/${modelId}`);
+  };
*** End Patch
*** Begin Patch
*** Update File: client/src/pages/marketplace.tsx
@@
-import { useLocation } from "wouter";
+import { useLocation } from "wouter";
@@
-  // Add or update the handleModelClick used by model cards:
-  const handleModelClick = (model: any) => {
-    // keep special case redirects if they exist on other parts of the app
-    if (model.id === 5) {
-      window.location.href = "/forecasting-model";
-      return;
-    } else if (model.id === 9) {
-      window.location.href = "/hrp-portfolio-optimization";
-      return;
-    }
-    // canonical model detail route
-    window.location.href = `/model/${model.id}`;
-  };
+  // Add or update the handleModelClick used by model cards:
+  const [, navigate] = useLocation();
+  const handleModelClick = (model: any) => {
+    // preserve existing special-case full-page redirects
+    if (model.id === 5) {
+      window.location.href = "/forecasting-model";
+      return;
+    } else if (model.id === 9) {
+      window.location.href = "/hrp-portfolio-optimization";
+      return;
+    }
+    // navigate using SPA routing to the canonical model detail route
+    navigate(`/model/${model.id}`);
+  };
*** End Patch
*** End Patch
```

Apply the patch
1. Save the patch file (example: feat-tests-model-id-resolution.patch).
2. From your repo root run:
   - git apply feat-tests-model-id-resolution.patch
   - git add client/src/pages/__tests__/model-detail.test.tsx client/src/components/layout/header-broken.tsx client/src/pages/marketplace.tsx
   - git commit -m "test(model-detail): add idParam resolution tests; standardize model routing to /model/:id"
   - git checkout -b feat/tests/model-id-resolution
   - git push --set-upstream origin feat/tests/model-id-resolution

Create a PR (optional, using GitHub CLI)
- gh pr create --title "test(model-detail): idParam resolution tests + standardize model navigation" --body "Adds integration tests asserting ModelDetail resolves id from /model/:id, /marketplace/:id and /models/:id. Standardizes navigation to canonical /model/:id from header search and marketplace cards. Keeps existing special-case redirects." --base main

Notes
- If your Jest config does not resolve the "@" path alias, update the test's jest.mock path to the relative path "../../lib/queryClient" or adjust moduleNameMapper in jest config.
- If you'd prefer I include a git commit/patch that also runs eslint/prettier formatting on the modified files, tell me and I'll update the patch.
- Want me to open the PR for you? I can provide the gh command (above) for you to run locally or run it here if you enable GitHub CLI access.