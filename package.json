{
  "name": "gefi-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bundle exec jekyll build",
    "deploy": "bundle exec jekyll build && wrangler pages deploy _site --project-name=gefi --branch=main",
    "preview": "bundle exec jekyll build && wrangler pages dev _site"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
